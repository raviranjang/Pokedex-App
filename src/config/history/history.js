import { createBrowserHistory } from "history";

// const client = process.env.REACT_APP_CLIENT || "common";

// const basename = `${client}`;
const history = createBrowserHistory();

export { history };